#include "Guidance.h"

CGuidance::CGuidance()
{


    // cvui::init(CANVAS_NAME);
}

CGuidance::~CGuidance()
{

    // image_server.stop();
}

void CGuidance::update()
{
    // image_server.set_txim(_display_im);
}

/*
void CGuidance::image_thread(CServer* image_server)
{
    // Start Image server
    image_server->start(4618);
}
*/
